# دليل اختبار الدفع بالفيزا

## خطوات اختبار الدفع

### 1. الحصول على مفاتيح Stripe للاختبار

**الطريقة السريعة:**
1. اذهب إلى [https://dashboard.stripe.com/test/apikeys](https://dashboard.stripe.com/test/apikeys)
2. سجل دخول أو أنشئ حساب جديد (مجاني تماماً)
3. انسخ المفاتيح التالية:
   - **Publishable key** (يبدأ بـ `pk_test_`)
   - **Secret key** (يبدأ بـ `sk_test_`)

### 2. إعداد ملف `.env.local`

أنشئ ملف `.env.local` في جذر المشروع وأضف:

```env
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
```

**مثال:**
```env
STRIPE_SECRET_KEY=sk_test_51AbCdEfGhIjKlMnOpQrStUvWxYz1234567890
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51AbCdEfGhIjKlMnOpQrStUvWxYz1234567890
```

### 3. تشغيل السيرفر

```bash
pnpm dev
```

### 4. اختبار الدفع

#### أ. أضف منتجات للسلة
- اذهب إلى الصفحة الرئيسية
- أضف منتجات للسلة

#### ب. اذهب لصفحة الدفع
- اضغط على أيقونة السلة
- اضغط على زر "Checkout"

#### ج. استخدم بطاقة اختبار Stripe

**بطاقة فيزا ناجحة:**
- **رقم البطاقة:** `4242 4242 4242 4242`
- **تاريخ الانتهاء:** أي تاريخ في المستقبل (مثل `12/25`)
- **CVV:** أي 3 أرقام (مثل `123`)
- **الرمز البريدي:** أي 5 أرقام (مثل `12345`)

**بطاقات اختبار أخرى:**

| السيناريو | رقم البطاقة | النتيجة |
|-----------|-------------|---------|
| دفع ناجح | `4242 4242 4242 4242` | ✅ نجاح |
| دفع مرفوض | `4000 0000 0000 0002` | ❌ رفض |
| يتطلب المصادقة | `4000 0025 0000 3155` | 🔐 يحتاج 3D Secure |
| بطاقة منتهية | `4000 0000 0000 0069` | ❌ منتهية |

### 5. ملء بيانات النموذج

1. **معلومات العميل:**
   - الاسم: أي اسم (مثل `Ahmed Ali`)
   - البريد الإلكتروني: أي بريد (مثل `test@example.com`)

2. **العنوان:**
   - العنوان: أي عنوان (مثل `123 Main St`)
   - المدينة: أي مدينة (مثل `Cairo`)
   - الرمز البريدي: `12345`
   - الدولة: اختر من القائمة

3. **بيانات البطاقة:**
   - استخدم البيانات أعلاه

4. **اضغط "Pay"**

### 6. النتيجة المتوقعة

✅ **عند النجاح:**
- ستظهر رسالة "Payment Successful!"
- سيتم تفريغ السلة تلقائياً
- سيتم إعادة التوجيه للصفحة الرئيسية

❌ **عند الفشل:**
- ستظهر رسالة خطأ توضح السبب
- يمكنك المحاولة مرة أخرى

## استكشاف الأخطاء

### خطأ: "Stripe secret key not configured"
**الحل:** تأكد من إضافة `STRIPE_SECRET_KEY` في ملف `.env.local`

### خطأ: "Invalid API Key"
**الحل:** تأكد من نسخ المفاتيح بشكل صحيح من Stripe Dashboard

### خطأ: "No such payment_intent"
**الحل:** أعد تحميل الصفحة وحاول مرة أخرى

### لا يظهر نموذج البطاقة
**الحل:** 
- تأكد من إضافة `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
- أعد تشغيل السيرفر بعد إضافة المتغيرات

## ملاحظات مهمة

⚠️ **مفاتيح الاختبار فقط:** المفاتيح التي تبدأ بـ `test_` للاختبار فقط ولا تقوم بمعاملات حقيقية

⚠️ **لا حاجة لبطاقة حقيقية:** يمكنك استخدام أي بيانات في بطاقة الاختبار

⚠️ **أعد تشغيل السيرفر:** بعد إضافة `.env.local`، أعد تشغيل `pnpm dev`

## رابط مفيد

- [Stripe Testing Cards](https://stripe.com/docs/testing)
- [Stripe Dashboard](https://dashboard.stripe.com/test/dashboard)

